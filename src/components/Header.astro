---
import { getRelativeLocaleUrl } from "astro:i18n";
import IconMenuDeep from "@/assets/icons/IconMenuDeep.svg";
import IconSunHigh from "@/assets/icons/IconSunHigh.svg";
import IconSearch from "@/assets/icons/IconSearch.svg";
import IconMoon from "@/assets/icons/IconMoon.svg";
import IconX from "@/assets/icons/IconX.svg";
import { stripBase, stripLocale } from "@/utils/withBase";
import config from "@/config";

const { site } = config;
const locale = Astro.currentLocale ?? config.site.lang;

// Strip the base prefix so isActive comparisons work regardless of deploy subdirectory.
const relativePath = stripBase(Astro.url.pathname);
const pathWithoutTrailingSlash =
  relativePath.endsWith("/") && relativePath !== "/"
    ? relativePath.slice(0, -1)
    : relativePath;

const currentPath = stripLocale(pathWithoutTrailingSlash, locale);

const isActive = (path: string) => {
  const currentPathArray = currentPath.split("/").filter(p => p.trim());
  const pathArray = path.split("/").filter(p => p.trim());
  return currentPath === path || currentPathArray[0] === pathArray[0];
};

const homeHref = getRelativeLocaleUrl(locale, "");
const blogHref = getRelativeLocaleUrl(locale, "posts");
const academicHref = getRelativeLocaleUrl(locale, "academic");
const aboutHref = getRelativeLocaleUrl(locale, "about");
const searchHref = getRelativeLocaleUrl(locale, "search");

const lifeLinks = [
  { label: "Musics", href: getRelativeLocaleUrl(locale, "musics") },
  { label: "Books", href: getRelativeLocaleUrl(locale, "books") },
  { label: "Movies", href: getRelativeLocaleUrl(locale, "movies") },
];

const lifeActive = lifeLinks.some(l => isActive(l.href));
const navItemClass =
  "rounded-lg px-3 py-2 text-sm font-medium transition-colors hover:text-accent";
---

<a
  id="skip-to-content"
  href="#main-content"
  class="bg-background text-accent absolute inset-s-16 -top-full z-50 px-3 py-2 backdrop-blur-lg transition-all focus:top-4"
>
  Skip to content
</a>

<header id="site-header" class="app-layout-wide sticky top-3 z-40">
  <nav
    id="site-nav"
    class="flex items-center justify-between rounded-2xl border border-transparent px-2 py-1.5 transition-all duration-300 [&.not-top]:border-border [&.not-top]:bg-background/70 [&.not-top]:shadow-sm [&.not-top]:backdrop-blur-xl"
  >
    <a
      href={homeHref}
      class="brand-link z-30 shrink-0 px-2 py-2 text-xl font-semibold whitespace-nowrap"
    >
      {site.title}
    </a>

    <!-- Desktop navigation -->
    <ul class="hidden items-center gap-1 md:flex">
      <li>
        <a href={blogHref} class:list={[navItemClass, { "active-nav": isActive("/posts") }]}>
          Blog
        </a>
      </li>
      <li>
        <a
          href={academicHref}
          class:list={[navItemClass, { "active-nav": isActive("/academic") }]}
        >
          Academic
        </a>
      </li>
      <li class="group relative">
        <button
          type="button"
          class:list={[
            navItemClass,
            "inline-flex items-center gap-1",
            { "active-nav": lifeActive },
          ]}
          aria-haspopup="true"
          aria-expanded="false"
        >
          Life
          <svg
            aria-hidden="true"
            class="size-3.5 transition-transform duration-200 group-hover:rotate-180"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="m6 9 6 6 6-6"></path>
          </svg>
        </button>
        <div
          class="invisible absolute top-full left-1/2 z-50 mt-1 w-44 -translate-x-1/2 rounded-xl border border-border bg-background/90 p-1 opacity-0 shadow-lg backdrop-blur-xl transition-all duration-150 group-hover:visible group-hover:opacity-100 group-focus-within:visible group-focus-within:opacity-100"
        >
          {
            lifeLinks.map(link => (
              <a
                href={link.href}
                class:list={[
                  "block rounded-lg px-3 py-2 text-sm transition-colors hover:bg-muted hover:text-accent",
                  { "text-accent": isActive(link.href) },
                ]}
              >
                {link.label}
              </a>
            ))
          }
        </div>
      </li>
      <li>
        <a
          href={aboutHref}
          class:list={[navItemClass, { "active-nav": isActive("/about") }]}
        >
          About
        </a>
      </li>
    </ul>

    <div class="flex items-center gap-0.5">
      <a
        href={searchHref}
        class:list={[
          "hidden rounded-lg p-2 transition-colors hover:text-accent sm:inline-flex",
          { "active-nav": isActive("/search") },
        ]}
        title="Search"
        aria-label="Search"
      >
        <IconSearch class="size-5" />
      </a>
      <button
        id="theme-btn"
        class="hover:[&>svg]:stroke-accent relative size-10 p-2.5"
        title="Toggle theme"
        aria-label="auto"
        aria-live="polite"
      >
        <IconMoon class="absolute top-[50%] left-[50%] translate-[-50%] scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
        <IconSunHigh class="absolute top-[50%] left-[50%] translate-[-50%] scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
      </button>
      <button
        id="menu-btn"
        class="focus-outline p-2 md:hidden"
        aria-label="Open menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <IconX id="close-icon" class="hidden size-5" />
        <IconMenuDeep id="menu-icon" class="size-5" />
      </button>
    </div>
  </nav>

  <!-- Mobile menu -->
  <div
    id="mobile-menu"
    class="mt-2 hidden rounded-2xl border border-border bg-background/90 p-2 shadow-lg backdrop-blur-xl md:hidden"
  >
    <a href={blogHref} class:list={["block rounded-lg px-3 py-2 text-sm font-medium hover:bg-muted hover:text-accent", { "text-accent": isActive("/posts") }]}>Blog</a>
    <a href={academicHref} class:list={["block rounded-lg px-3 py-2 text-sm font-medium hover:bg-muted hover:text-accent", { "text-accent": isActive("/academic") }]}>Academic</a>
    <p class="px-3 pt-3 pb-1 text-xs font-semibold tracking-wide text-muted-foreground uppercase">
      Life
    </p>
    {
      lifeLinks.map(link => (
        <a
          href={link.href}
          class:list={[
            "block rounded-lg px-3 py-2 ps-6 text-sm hover:bg-muted hover:text-accent",
            { "text-accent": isActive(link.href) },
          ]}
        >
          {link.label}
        </a>
      ))
    }
    <a href={aboutHref} class:list={["block rounded-lg px-3 py-2 text-sm font-medium hover:bg-muted hover:text-accent", { "text-accent": isActive("/about") }]}>About</a>
    <a href={searchHref} class:list={["block rounded-lg px-3 py-2 text-sm font-medium hover:bg-muted hover:text-accent sm:hidden", { "text-accent": isActive("/search") }]}>Search</a>
  </div>
</header>

<script>
  function initHeader() {
    const header = document.querySelector("#site-nav");
    const menuBtn = document.querySelector<HTMLButtonElement>("#menu-btn");
    const mobileMenu = document.querySelector("#mobile-menu");
    const menuIcon = document.querySelector("#menu-icon");
    const closeIcon = document.querySelector("#close-icon");

    const onScroll = () => {
      const scrolled = (window.scrollY || document.documentElement.scrollTop) > 8;
      header?.classList.toggle("not-top", scrolled);
    };
    onScroll();
    window.addEventListener("scroll", onScroll, { passive: true });

    const toggleMenu = () => {
      if (!menuBtn || !mobileMenu) return;
      const open = menuBtn.getAttribute("aria-expanded") === "true";
      menuBtn.setAttribute("aria-expanded", open ? "false" : "true");
      menuBtn.setAttribute("aria-label", open ? "Open menu" : "Close menu");
      mobileMenu.classList.toggle("hidden");
      menuIcon?.classList.toggle("hidden");
      closeIcon?.classList.toggle("hidden");
    };

    menuBtn?.addEventListener("click", toggleMenu);

    return () => {
      window.removeEventListener("scroll", onScroll);
      menuBtn?.removeEventListener("click", toggleMenu);
    };
  }

  let cleanup: (() => void) | undefined;
  document.addEventListener("astro:page-load", () => {
    cleanup?.();
    cleanup = initHeader();
  });
</script>
